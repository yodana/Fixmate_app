## Setup

Cloner le depot

Installer les dépendances avec `npm install`

Setup une bdd puis mettre les parametres correpondants dans `node_api/server.js` et `scripts/setup.js`

Lancer npm run db:setup

Lancer l'api avec `node node_api/server.js`

Lancer l'application avec `npm run dev`

